# Delta for Pi Provider Adapter

## Normative location

This requested change-level entry point normatively delegates to the delta at [`specs/pi-provider-adapter/spec.md`](specs/pi-provider-adapter/spec.md). All requirements, RFC 2119 obligations, scenarios, and specified payload encodings in that file apply to `add-pi-tool-support` without modification.

The nested path is the canonical executable OpenSpec delta because `openspec/specs/pi-provider-adapter/spec.md` already exists. This compatibility entry point MUST NOT be interpreted as a separate or conflicting delta during archive.
